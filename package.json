{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "example": "pulp browserify --main Example.Main --include example/src --optimise --to example/dist/example.js"
  },
  "devDependencies": {
    "pulp": "^12.3.1",
    "purescript": "^0.12.3",
    "purescript-psa": "^0.7.3",
    "rimraf": "^2.6.3"
  }
}
